


    spring:
        data:
          mongodb:
             host: localhost
             port: 27017
             database: my-db2

        application:
              name: USER-SERVICE

              eureka:
                  client:
                      register-with-eureka: true
                      fetch-registry: true
                      service-url:
                          defaultZone: http://localhost:8761/eureka/
                  instance:
                      host-name: localhost